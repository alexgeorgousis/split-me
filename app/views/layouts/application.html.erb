<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Split Me" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 text-gray-100">
    <% if Current.user&.demo? %>
      <div class="bg-yellow-600 text-black text-center py-2 px-12 font-medium relative" data-controller="dismissable">
        <div>This is a demo. Your Splits will be lost when your session expires. Make the most of it!</div>
        <div class="text-sm"><%= Current.user.minutes_remaining %> minutes remaining</div>
        <button data-action="click->dismissable#hide" class="absolute right-2 top-1/2 -translate-y-1/2 text-2xl leading-none hover:opacity-70 w-8 h-8 rounded-full border-2 border-black flex items-center justify-center cursor-pointer">&times;</button>
      </div>
    <% end %>

    <header>
      <% if Current.user %>
        <nav class="flex justify-end items-center gap-2 p-4 border-b border-gray-800">
          <%= link_to favourites_path, class: "p-2 hover:bg-gray-800 rounded-full transition cursor-pointer" do %>
            <%= icon controller_name == "favourites" ? "heart_filled" : "heart_unfilled", css_class: "w-6 h-6 text-gray-300" %>
          <% end %>
          <div class="h-6 w-px bg-gray-700"></div>
          <%= button_to session_path, method: :delete, class: "p-2 hover:bg-gray-800 rounded-full transition cursor-pointer" do %>
            <%= icon "logout", css_class: "w-6 h-6 text-gray-300" %>
          <% end %>
        </nav>
      <% end %>
    </header>

    <main class="container mx-auto mt-14 px-3 flex">
      <%= yield %>
    </main>
  </body>
</html>
