<% content_for :title, "New split" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New split</h1>

  <%= render "form", split: @split %>

  <%= link_to "Back to splits", splits_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-700 hover:bg-gray-600 inline-block font-medium" %>
</div>
